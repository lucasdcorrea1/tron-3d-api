# MongoDB
MONGO_URI=mongodb://localhost:27017
DB_NAME=tron_3d

# JWT (same secret as tron-legacy-api)
JWT_SECRET=change-me-in-production

# Server
PORT=8090

# Frontend URL (CORS)
FRONTEND_URL=https://whodo.com.br

# Admin user IDs (comma-separated MongoDB ObjectID hex strings)
ADMIN_USER_IDS=
